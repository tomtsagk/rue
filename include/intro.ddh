# dark dimension intro
(class world_intro dd_world
	(group

	# tracking timing
	(def float counter)
	(def int positive)
	(def float delay)

	# dark dimension logo
	(def dd_meshTexture logo)
	(def dd_image logoTexture)

	(def dd_sound sound)
	(def int soundPlayed)

	(def float scale)

	(def avdl_program logoProgram)
	(def float logoAnim)
	(def float logoAnimPositive)
	(def float shaderTime)

	# constractor
	(function void create (group))
	(function void onload (group))
	(function void resize (group))

	# update - update animations
	(function void update (group float dt))

	# draw
	# zoom-out so the world is visible
	# rotate the world
	# draw the cube
	(function void draw (group))

	(function void key_input (group int key))

	(function void clean (group))

	)
) # main world
